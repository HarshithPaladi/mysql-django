version: '3'
services:
  mysql_host:
    image: mysql
    restart: always
    environment:
      - MYSQL_ROOT_PASSWORD=admin
      - MYSQL_DATABASE=mysql_db
    healthcheck:
        test: ["CMD","mysqladmin","ping","-h","localhost"]
        timeout: 10s
        retries: 5
  django:
    image: djangoharshith:1
    restart: always
    ports:
      - "8888:8000"
    depends_on:
        mysql_host:
          condition: service_healthy
